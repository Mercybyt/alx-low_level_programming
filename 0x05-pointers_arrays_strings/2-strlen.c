#include "holberton.h"

/**
 * _strlen - function that return the length of a string
 *@s: declaration of *s and parameters
 *Return: Always 0
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != 0; ++i)
	{
	}
	return (i);
}
