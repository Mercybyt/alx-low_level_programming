#include "holberton.h"
/**
 * add - adds two integers and returns the result
 *@a: is the int that we use for returning the sum
 *@b: is the int that we use for returning the sum
 *Return: 0
 */
int add(int a, int b)
{
	return (a + b);
}
